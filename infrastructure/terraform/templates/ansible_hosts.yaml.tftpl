# TODO: align the references to hosts between Terraform and
#       Ansible, as one uses hostnames and the other the OS.
#       The current approach is not scalable.

bastion:
  hosts:
    openbsd:
      ansible_host: "${ansible_hosts_ipaddr.openbsd}"

webservers:
  hosts:
    debian:
      ansible_host: "${ansible_hosts_ipaddr.debian}"
    rocky:
      ansible_host: "${ansible_hosts_ipaddr.rocky}"
